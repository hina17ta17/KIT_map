# 作図ツールの使い方

`http://localhost:3000`

起動していない場合：
```powershell
cd "c:\Users\a-hinata\プログラミング\Hackit\マップ\web"
npm run dev
```

---

## 画面の見方

```
┌──────────────────────────────────────────────────────┐
│              [淡色地図][航空写真][標準地図]   ← 上部中央 │
├────────────────────┬─────────────────────────────────┤
│ KIT MAP — 作図ツール │                          [+][−] │ ← 拡大縮小
│                    │                                 │
│ ┌────────────────┐ │                                 │
│ │[敷地を描く]     │ │                                 │
│ │[建物を描く]     │ │            地  図                │
│ └────────────────┘ │                                 │
│ ┌────────────────┐ │                                 │
│ │基盤地図情報から  │ │                                 │
│ │取り込む         │ │                                 │
│ │[XMLを選ぶ]     │ │                                 │
│ └────────────────┘ │                                 │
│                    │                                 │
│ 敷地（0）           │                                 │
│ 建物（0）           │                                 │
│                    │                                 │
│ ┌────────────────┐ │                                 │
│ │書き出し/読み込み │ │                                 │
│ └────────────────┘ │                                 │
│                    │ [縮尺]              [地理院タイル]│
└────────────────────┴─────────────────────────────────┘
```

---

## STEP 0：キャンパスを画面に出す

1. 上部中央の **[航空写真]** を押す（作図中はこれ）
2. 初期位置は**おおよその位置**なので、地図を動かして対象範囲に合わせる

| 操作 | 方法 |
|---|---|
| 移動 | 地図をドラッグ |
| 拡大縮小 | マウスホイール／右上の [+] [−] |
| 作図しやすい倍率 | **建物1棟が画面の1/4くらいに見える程度**（z17〜18） |

---

## STEP 1：敷地を描く

**これを最初にやる。** 基盤地図情報の取り込みは、敷地を「切り抜きの型」として使うため。

1. **[敷地を描く]** を押す
   → カーソルが十字になり、パネルが「敷地を作図中 — 頂点 0」に変わる
2. キャンパスの外周を**クリックして頂点を打っていく**
   - 角のたびにクリック。曲線は数点で近似すればよい
   - 最後の点と最初の点は**自動でつながる**ので、一周させなくてよい
3. **[確定]**（または `Enter`）

| キー | 動作 |
|---|---|
| `Enter` | 確定 |
| `Backspace` | 直前の頂点を取り消す |
| `Esc` | 作図全体を取り消す |

> 頂点が3つ未満だと [確定] は押せません。
> 描いた敷地は緑の破線で表示されます。多少ざっくりでも構いません（後で描き直せます）。

---

## STEP 2：建物を用意する（2つの方法）

### 方法A：基盤地図情報から取り込む（推奨・速い）

敷地を描くと **[XMLを選ぶ（複数可）]** が押せるようになる。

1. 国土地理院「基盤地図情報ダウンロードサービス」で
   **基本項目 → 対象メッシュ → 建築物** をダウンロード（無料・要ユーザー登録）
2. **ZIPを展開**して `.xml` を取り出す（ツールはZIPではなくXMLを受け取る）
3. **[XMLを選ぶ]** で選択。キャンパスが複数メッシュにまたがるなら**まとめて複数選択**
4. **敷地の内側にある建物だけ**が自動で取り込まれ、`B-01, B-02…` の仮IDが振られる

取り込み結果は青いパネルに件数で表示される。
うまくいかない場合は警告が出るので、そのメッセージを見て判断する。

#### 基盤地図情報のダウンロード手順

> 画面構成は変更されることがあるため、**ボタンの位置ではなく名称で探すこと。**

##### 1. ユーザー登録（初回のみ・無料）

1. 「基盤地図情報ダウンロードサービス」を開く（`fgd.gsi.go.jp/download/`）
2. **新規登録**からアカウントを作る（メールアドレス・ID・パスワード）
3. 登録したIDでログインする

> ダウンロード時にログインが求められる。先に済ませておくと途中で止まらない。

##### 2. 「基盤地図情報 基本項目」を選ぶ

トップページに以下の選択肢がある。**一番上の「基本項目」**を選ぶ。

```
✅ 基盤地図情報 基本項目          ← これ
❌ 基盤地図情報 数値標高モデル      （標高。今回は不要）
❌ 基盤地図情報 ジオイド・モデル    （不要）
```

##### 3. 範囲を選ぶ

メッシュ（区画）の選択画面になる。

> **扇が丘キャンパスのメッシュは `543665` です。**（実測で確認済み）
>
> | メッシュ | 覆う範囲 | 扇が丘 |
> |---|---|---|
> | `543664` | 経度 136.5000〜136.6250 / 緯度 36.5000〜36.5833 | ❌ 入っていない |
> | **`543665`** | 経度 136.6250〜136.7500 / 緯度 36.5000〜36.5833 | ✅ **これ** |
>
> キャンパス西端が境界（経度 136.625）に近いため、
> **`543664` も一緒に取っておく**と取りこぼしがない。

**方法①：地図から選ぶ（推奨）**
1. 地図を扇が丘キャンパス付近まで移動・拡大する
2. キャンパスに重なるメッシュの四角を**クリックして選択**（選択された区画は色が変わる）
3. 選択したメッシュの番号が `543665` になっているか**必ず確認する**

**方法②：市区町村から選ぶ**
1. 「都道府県または市区町村から選択」を開く
2. **石川県 → 野々市市** を選ぶ
3. 扇が丘は金沢市との境界付近なので、**金沢市も追加**しておくと安全

> 余分に取っても害はない。作図ツールが敷地の外側を自動で捨てるため。

##### 4. 項目を「建築物」に絞る

チェックボックスの一覧が出る。**「建築物の外周線」だけにチェック**を入れる。

```
□ 測量の基準点
□ 海岸線
□ 水涯線
☑ 建築物の外周線      ← これだけ
□ 道路縁
□ 軌道の中心線
□ 標高点
□ 等高線
□ 行政区画の境界線及び行政区画代表点
```

他も選べるがファイルが増えるだけで使わない。

##### 5. 形式を確認してダウンロード

- 形式は **JPGIS(GML)形式**（作図ツールはこれを読む）
- 「ダウンロードファイル確認へ」→ ファイル一覧が出る
- 「全てチェック」→「まとめてダウンロード」

##### 6. 展開して XML を取り出す

ダウンロードした ZIP を展開する。**中にさらに ZIP が入っている場合は、それも展開する。**

展開後、以下のようなファイルが出る：

```
FG-GML-543665-BldA-20260701-0001.xml   ★これを使う（建築物の「面」・約90MB）
FG-GML-543665-BldL-20260701-0001.xml   建築物の「線」。使わない
fmdid26-0701.xml                       メタデータ。使わない
```

**ファイル名に `BldA` が含まれるものが目的のファイル。**
`A` は Area（面）の意味で、ポリゴンデータが入っている。

##### 6.5. 生データの置き場所

展開したフォルダは **`マップ/raw/`** に置く。
`.gitignore` 済みなので、90MB のファイルがリポジトリに入る心配はない。

```
マップ/
├── raw/                              ← 生データ（git 追跡外）
│   └── FG-GML-543665-11-.../
│       └── FG-GML-543665-BldA-....xml
└── web/public/data/                  ← 変換後の GeoJSON（git 追跡する）
```

##### 7. 取り込む

作図ツールの **[XMLを選ぶ（複数可）]** で、`BldA` のファイルを**すべて選択**する。
ファイルダイアログから直接 `raw/` の中を選べばよい。**`web/` にコピーする必要はない。**

> 1メッシュに約10万棟あるが、敷地の外接矩形で先に切り捨てるため数秒で終わる。
> ボタンが「読み込み中…」に変わっている間は待つ。

##### チェックリスト

- [ ] メッシュ番号が **`543665`** になっている
- [ ] `BldA` を含む XML が手元にある（`BldL` ではない）
- [ ] ZIP ではなく展開済みの XML を選んでいる
- [ ] 生データを `マップ/raw/` に置いた
- [ ] 作図ツールで敷地を先に描いてある

---

### 方法B：手で描く

1. **[建物を描く]** を押す
2. 建物の角をクリックして囲む
3. **[確定]**（`Enter`）
4. 仮ID が自動で振られる

> 方法Aを試して、足りない建物だけ方法Bで足す、が最も速い。

---

## STEP 3：号館番号を入れる（現地確認の後）

**作図モードを抜けた状態**（[敷地を描く]／[建物を描く] が並んでいる状態）で操作する。

1. 地図上の**建物をクリック**する、または左のリストから選ぶ
   → 選んだ建物がオレンジになり、編集パネルが開く
2. **号館番号**に `23` と入れると、ラベルが `B-05` → `23号館` に変わる
3. 号館以外（図書館・体育館など）は **建物名**に直接入れる
4. **階数**は分かる範囲で（`0` = 未確認）
5. **メモ**に現地で気づいたことを残す

左のリスト見出しに **`号館番号 入力済 n`** と進捗が出る。

> 建物をクリックしても選択できない場合、作図モードのままになっている可能性がある。
> `Esc` を押してから試す。

---

## STEP 4：書き出す

1. **[campus.geojson]** **[buildings.geojson]** を押してダウンロード
2. ダウンロードした2ファイルを **`web/public/data/`** に置く
3. git にコミットする

### ⚠ 必ず書き出すこと

作業内容はブラウザに自動保存される（localStorage キー `kitmap.draft.v1`）が、
**ブラウザのデータを消すと失われる。** 区切りごとに必ず書き出す。

一度書き出したファイルは **[読み込み]** で復元できる。

---

## よくある詰まり

| 症状 | 原因と対処 |
|---|---|
| [XMLを選ぶ]が灰色で押せない | 敷地が未作成。STEP 1 を先に行う |
| クリックしても頂点が増えない | 作図モードに入っていない。[敷地を描く]／[建物を描く] を押す |
| 建物をクリックしても選択できない | 作図モードのまま。`Esc` を押す |
| 「敷地内に該当なし」と出る | 対象メッシュ違い。扇が丘は `543665`。`543664` ではない |
| 「座標が日本の範囲外です」と出る | XMLの形式が想定と異なる。メッセージごと共有すれば調整する |
| 航空写真が粗い／新しい建物が無い | 撮影年次の問題。足りない分は方法Bで手描きする |

---

## 作業の目安

| STEP | 所要 |
|---|---|
| 敷地を描く | 15分 |
| 基盤地図情報の取り込み | 30分（ダウンロード含む） |
| 号館番号の入力 | 半日（現地確認と同時） |
