@AGENTS.md

# KIT MAP — 金沢工業大学 扇が丘キャンパスの案内サイト

新しいセッションはここから読むこと。詳細は `docs/` にある。

| 読むもの | 中身 |
|---|---|
| `docs/仕様.md` | **最終的に作るもの**。フェーズ構成と、やらないと決めたこと |
| `docs/データ構造.md` | GeoJSON / JSON のスキーマ全部 |
| `docs/作業手順.md` | 作図ツールの使い方と、残っている作業 |
| `USAGE.md` | 作図ツールの操作マニュアル |

## いま何を作っているか

**フェーズ1：現在地から目的地（建物・教室）までの経路案内。**
ログイン不要・データベース不要・費用0円で動く。

Google Directions API は構内の通路も教室も知らないため使えない。
**経路は自前のグラフ（チェックポイント＋接続）で A* を回して計算する。**
これがこの製品の存在理由。

## 進捗

| | 状態 |
|---|---|
| 作図ツール（`/`） | ✅ 動作中 |
| 敷地の外周 | ✅ 1件・18頂点 |
| 建物・屋外 | ✅ 38件・全件命名済み |
| チェックポイント / 接続 / 部屋 の入力機能 | ✅ 実装済み |
| チェックポイントと接続のデータ | ⏳ 入力待ち |
| 現在地・検索・A*・経路描画・到着判定 | ❌ 未実装 |

**データ（チェックポイントと接続）が揃うまで、経路探索の実装には入れない。**

## 絶対に間違えてはいけないこと

- **基盤地図情報の `posList` は「緯度 経度」の順。** GeoJSON は `[経度, 緯度]` なので入れ替える。
  間違えると日本のデータが南米沖に飛ぶ。`lib/fgd.ts` で対応済み
- **扇が丘キャンパスのメッシュ番号は `543665`。** `543664` は西隣で範囲外（実測確認済み）
- **地図の置き場所は `position: absolute` をインラインで指定すること。**
  MapLibre が付ける `.maplibregl-map` の `position: relative` が
  Tailwind の `.absolute` を後勝ちで上書きし、高さが 0 に潰れる
- **地理院タイルの出典表示は義務。** 画面から消さない
- **Google マップの航空写真をトレース元にしてはいけない**（規約違反）。地理院の航空写真を使う

## ★ プッシュ先に注意

| | |
|---|---|
| **いまの push 先** | `https://github.com/hina17ta17/map-sakusei`（作図・開発用） |
| **最終版の push 先** | **別リポジトリ（未定）。ここへは push しない** |

**最終的に公開するサイトも同じこのフォルダで作る。**
完成版を出すときは push 先を切り替えること。**勝手に `origin` へ push しない。**

## 開発

```powershell
npm install
npm run dev      # http://localhost:3000
npm run build    # 型チェック込み
```

`http://localhost:3000/test` は地図が表示されないときの診断ページ。
